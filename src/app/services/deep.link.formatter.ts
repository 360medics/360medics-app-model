class DeepLinkFormatter {

        toHttp(link: string) {
            switch (link) {
                case 'value':
                    
                    break;
            
                default:
                    break;
            }
        }
}

export const DeepLinkFormatterSingleton = new DeepLinkFormatter();